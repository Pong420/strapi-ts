{
  "name": "strapi-ts",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server": "yarn workspace @strapi-ts/app",
    "start": "yarn server start",
    "dev": "yarn concurrently -n ts,strapi \"yarn tsup --watch\" \"yarn server develop\"",
    "build": "yarn tsup",
    "postinstall": "node tsup.patch.js"
  },
  "workspaces": {
    "packages": [
      "strapi",
      "app"
    ]
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "tsup": "^4.12.5"
  }
}
