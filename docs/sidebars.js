// @ts-check
// https://docusaurus.io/docs/sidebar

/**
 * @typedef {string | { type: 'doc'; id: string; label: string; }} SidebarItemDoc
 * @typedef {{ type: 'link'; label: string; href: string; }} SidebarItemLink
 * @typedef {{ type: 'autogenerated'; dirName: string; }} SidebarItemAutogenerated
 * @typedef {{ type: 'category'; label: string; items: SidebarItem[]; collapsed?: boolean; }} SidebarItemCategory
 * @typedef {SidebarItemDoc | SidebarItemLink | SidebarItemAutogenerated | SidebarItemCategory} SidebarItem
 * @typedef {SidebarItem[] | Record<string, SidebarItem[]>} Sidebar
 */

/** @type {Record<string, Sidebar>} */
const config = {
  docsSidebar: [
    'getting-started',
    {
      type: 'category',
      label: 'Guide',
      items: [{ type: 'autogenerated', dirName: 'guide' }],
      collapsed: false
    },
    'helper-scripts',
    'test'
  ],
  apiSidebar: [
    {
      type: 'autogenerated',
      dirName: 'api' // generate sidebar slice from the docs folder (or versioned_docs/<version>)
    }
  ]
};

module.exports = config;
